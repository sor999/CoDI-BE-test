spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/codi_db
    username: root
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    database: postgresql
    hibernate:
      ddl-auto: create